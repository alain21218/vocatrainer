import { WithCommentsDirective } from './with-comments.directive';

describe('WithCommentsDirective', () => {
  it('should create an instance', () => {
    const directive = new WithCommentsDirective();
    expect(directive).toBeTruthy();
  });
});
